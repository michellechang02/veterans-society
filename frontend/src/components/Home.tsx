import React from 'react';

interface HomeProps {
  // Define props here if any exist
}

const Home: React.FC<HomeProps> = (props) => {
  return (
    <div>
      <h1>Home!!!</h1>
    </div>
  );
}

export default Home;
